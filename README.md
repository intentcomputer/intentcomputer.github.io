# Intent Computer

Static GitHub Pages site for `intentcomputer.github.io`.

The page is built with plain HTML, CSS, and JavaScript so the organization Pages repo can publish directly from the default branch.
